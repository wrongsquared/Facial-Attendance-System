
  # Attendify UI/UX Development

  This is a code bundle for Attendify UI/UX Development. The original project is available at https://www.figma.com/design/4DnJKGyxzgiEgeCV4PBHgv/Attendify-UI-UX-Development.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  